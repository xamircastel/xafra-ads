# Configuración para Google App Engine
server.port=${PORT:8080}

# Base de datos PostgreSQL en GCP
database.driver=org.postgresql.Driver
database.url=jdbc:postgresql://${DB_HOST:34.28.245.62}:${DB_PORT:5432}/${DB_NAME:xafra-ads}
database.user=${DB_USER:postgres}
database.password=${DB_PASSWORD:XafraTech2025!}

# Configuración de seguridad
password.encription=%a7ra-.passd0474
user.encription=xafra2-ads-encrytion3

# Configuración de límites
limit.subscribe=250
limit.topup=10
file.name.topup=TOPUP_{date}.csv
file.path.topup=/tmp/topup/

# Configuración de logging para GCP
logging.level.root=INFO
logging.level.com.develop.job=DEBUG
logging.pattern.console=%d{HH:mm:ss.SSS} [%thread] %-5level %logger{36} - %msg%n

# Configuración de actuator para health checks
management.endpoints.web.exposure.include=health,info
management.endpoint.health.show-details=always
management.health.db.enabled=true
